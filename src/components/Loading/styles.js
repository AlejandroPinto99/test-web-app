import makeStyles from '@mui/styles/makeStyles';

export default makeStyles(() => ({
  loading: {
    width: '100%',
    top: '50%',
    display: 'grid',
    position: 'absolute',
    placeItems: 'center'
  }
}))